
describe("Invoices", () => {
  describe("Can create Invoice from WcOrder", () => {

    //const order = JsonConvert.toWcOrder(ordersJson[0]);
    //const invoice = Invoices.tryCreateInvoice(order);
  });
});
